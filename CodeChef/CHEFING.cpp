#include<iostream>
#include<vector>

using namespace std;

int main()
{
	int t;
	scanf("%d",&t);
	
	while(t-- != 0)
	{
		int n;
		vector<string> dish;
		
		vector<int>ingredient(26,0);
		
		for(int j=0;j<n;j++)
		{
			string a;
			scanf("%s",&a);
			for (int k=0;k<a.length();k++)
			{
				if
			}
		}
		
		for(int i = 0;i<ingredient.size();i++)
			cout<<ingredient[i]<<endl;
	}
	
	return 0;
}
